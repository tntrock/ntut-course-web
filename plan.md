# 北科課程網站 — 參考文件

`ntut-course-web` 的資料契約、架構決策與踩過的坑。

**這份文件記的是看程式碼看不出來的東西。** 功能長什麼樣子看程式碼，改了什麼看
`git log`；這裡只留下「為什麼」與「哪裡會咬人」。每一條資料事實都對線上資料驗證過。

網站說明與開發指令見 [README](./README.md)。

---

## 1. 資料契約

資料來自 [`ntut-course-crawler`](https://github.com/tntrock/ntut-course-crawler)
發布的靜態 JSON。`meta.json` 是所有其他請求的入口，**不加版本號、永遠網路優先**。

```
https://tntrock.github.io/ntut-course-crawler/meta.json
```

以下每一條都與直覺不同，而且都直接決定了某一塊程式怎麼寫。

### 1.1 學期字串在 `path`，不在 `semester`

`meta.semesters[]` 的欄位是 `year` / `sem` / `path`，**沒有 `semester`**。
陣列由新到舊，但要判斷最新學期一律用 `meta.latest`，不要依賴排序。

**絕不可把學期寫死在前端。**

每個學期的 `generated_at` 是該學期資料的產生時間，也是快取版本號的來源（§2.2）。
舊學期的值永遠不變，所以歷史資料只下載一次。

### 1.2 節次順序不是字典序

```
1  08:10   2  09:10   3  10:10   4  11:10
N  12:10   5  13:10   6  14:10   7  15:10
8  16:10   9  17:10   A  18:30   B  19:20   C  20:20   D  21:10
```

`4` 之後是午休 `N`，`9` 之後是夜間 `A`。字典序會說 `A` 比 `5` 早。

**任何跟「連續」「最早 / 最晚」有關的邏輯都必須讀 `meta.periods` 的陣列順序。**
連續判斷不能無腦取頭尾——把 `2、4` 顯示成 `2-4` 等於謊稱包含第 3 節
（見 `lib/formatTime.ts`）。

### 1.3 必選修是三態

`required` 是布林、`requirement_type` 是完整類別。**★ 和 ☆ 都是選修**，
差別在共同 / 專業：

| 符號 | `required` | `requirement_type` |
| ---- | ---------- | ------------------ |
| ○    | `true`     | 部訂共同必修       |
| △    | `true`     | 校訂共同必修       |
| ●    | `true`     | 部訂專業必修       |
| ▲    | `true`     | 校訂專業必修       |
| ☆    | `false`    | 共同選修           |
| ★    | `false`    | 專業選修           |

原始欄位空白時 `required` 是 **`null`，不是 `false`**。學分統計因此分成
必修 / 選修 / 未標示三欄——把 `null` 併進選修會讓試算悄悄算錯。

⚠️ 上表是**可能值**不是當期實際值。115-1 只出現其中 4 種。
**篩選選項必須由當期資料動態產生**，寫死六個按鈕會讓使用者看到永遠 0 筆的選項，
歷史學期出現新值也會漏掉。

### 1.4 授課語言是三態

115-1 實測 2,717 門課：`null`（中文）2,218、`英語` 488、`中英雙語` 11。

`中英雙語` 是實測才發現的，未來可能還有別的值——**篩選選項同樣由資料動態產生**，
不可寫成 `isEnglish` 布林。

### 1.5 教師以代碼識別

115-1 有 **803 個教師代碼但只有 801 個不同姓名**——確實有同名老師
（林志哲、陳盈竹各兩位）。

所有連結、收藏、分組一律用 `teacher_codes`，**姓名只拿來顯示**。

名稱與代碼是兩個平行陣列。實測長度全部對得上，但對不上時寧可顯示純文字也不要生出
連到別人頁面的連結——錯的連結比沒有連結糟（見 `components/course/CourseInfo.tsx`
的 `NameLink`）。

### 1.6 課號跨學期不通用

而且**合開的課，各班級是不同課號**（數位影像處理在資工四與資工所是兩個課號），
備註欄會寫明。

課表與收藏一律以 `{學期}:{課號}` 為 key，各學期獨立，**不做遷移**。

### 1.7 `stage` 是「階段」不是年級

學校原始表格的欄位是：

```
課號 | 課程名稱 | 階段 | 學分 | 時數 | 修 | 教師 | 日 一 二 三 四 五 六 | …
```

年級在**「班級」**那一欄（資工四）。115-1 全學期 2,878 門課：

|                                     |                  |
| ----------------------------------- | ---------------- |
| `stage` 為 `"1"`                    | 2,768 門（96%）  |
| 班級是「資工四」而 `stage` 為 `"1"` | 291 門，**全部** |
| 班級含「一」而 `stage` 為 `"3"`     | 6 門             |

零相關。各系所填法還不一致（體育專項（七）~（十一）全是 `3`；智動的國文在
二/三/四年級是 `3`/`5`/`7`）。**所以不猜語意**，用學校自己的欄位名，
`"1"` 不顯示（見 `lib/course.ts`）。

### 1.8 教學大綱有四種狀態

`syllabus.json` 有兩個來源要一起看：**更新中的學期在 `semesters[]` 逐課列出，
抓完的搬到 `frozen` 只留摘要**。只看一邊會漏掉九成的學期。

| 狀態                   | 判斷                              | 畫面                       |
| ---------------------- | --------------------------------- | -------------------------- |
| `semester-not-covered` | 該學期不在對照裡                  | 本學期未收錄               |
| `no-syllabus`          | `syllabus_url === null`（約 30%） | 這門課沒有大綱，分頁不顯示 |
| `pending`              | 有連結但還沒抓到                  | 尚未收錄，過一陣子再看     |
| `available`            | 對照表裡有，或該學期已凍結        | 顯示內容                   |

**涵蓋範圍用算的，不寫死**——爬蟲正在往回逐期補，寫死的範圍會在某天悄悄變錯，
而且讀起來仍然合理，不會有人發現（`lib/syllabus.ts` 的 `syllabusCoverage()`）。

兩個細節：

- 快取版本號要用**該門課的抓取時間**，不是學期的 `generated_at`。老師改大綱時
  學期索引不會重新產生，用學期版本號會讓修訂過的大綱永遠取到舊的。
  凍結學期則共用 `frozen.at`。
- 路由 loader 只在**逐課對照表確認檔案存在**時才預取。凍結學期沒有逐課對照，
  存在性得看 `syllabus_url`，光憑「學期凍結了」就預取等於對三成沒有大綱的課發 404。

大綱是平鋪的，**沒有 `extra` 欄位**（全掃 1,909 份沒有任何一份有）。
未知的頂層欄位一律列出來，學校加欄位時才不會憑空消失。

### 1.9 系所、學院與班級

- `colleges[].name` **可以是 `null`**，代表教務處 / 體育室 / 通識中心 / 師培中心 /
  校院級課程這 5 個不屬於任何學院的單位。UI 一律顯示成「校級單位」，
  **絕不可讓 `null` 或空字串跑到畫面上**。
- `class_groups` 是 `{id, name, url}[]`，**不是字串陣列**。
- C0/C2/C5/C7 掛的是院級共同課程，名字和上層學院一模一樣。用**資料的形狀**判斷
  （`college !== null && name.endsWith('學院')`），不寫死代碼——代碼會變，
  形狀不會（`lib/browse.ts` 的 `isCollegeWideUnit()`）。
- 教師分組要有「其他」這一組：非漢字開頭的姓名會全部落在外面（外籍教師），
  排在最後。

### 1.10 輕量索引沒有 `classrooms`

`{semester}/index.json` 是搜尋的資料來源，但它**沒有 `classrooms` 欄位** ——
教室只存在系所課程檔（`{semester}/courses/{dept}.json`）裡。

這件事會漏出到畫面上：從**搜尋結果**加入課表的課沒有教室，從**詳情頁**加入的有，
同一張課表於是有的顯示有的不顯示。`refreshSnapshot()` 也吃索引，所以按一下
「更新為最新資料」還會把原本有的教室洗掉。

處理方式（`hooks/useClassroomBackfill.ts`）：課表頁發現快照缺教室時，回頭抓那幾門
課所屬的系所檔補上。用「補進快照」而不是「顯示時才查」有兩個理由 —— 一次修好之後
**離線也看得到**，而且匯出的圖不會缺一半。補齊之後就不再發請求。

`refreshSnapshot()` 則只在新資料真的帶了教室時才覆蓋。

### 1.11 教室容量在另一個檔，而且一半是 `null`

`capacity.json` 是**根目錄的跨學期端點**（不是 `{semester}/` 底下），鍵是教室代碼
——與 `classrooms.json` 的 `id`、課程的 `classroom_codes` 同一組命名空間，實測
115-1 的 231 間 **0 筆對不上**。gzip 只有 7 KB。

```json
{
  "48": {
    "name": "三教307(e)",
    "full_name": "第三教學大樓307室",
    "capacity": 50,
    "checked_at": "…"
  }
}
```

⚠️ **`capacity` 是 `null` 的比例很高**：445 間裡 229 間沒有值。抽驗過學校原始頁面
——**那一格本來就是空白的**，不是抓取或解析失敗。分棟差異極大：

| 建築                      | 有容量 / 總數 |
| ------------------------- | ------------- |
| 三教 / 六教 / 先鋒 / 二教 | 全部有        |
| 共同                      | 14 / 18       |
| 科研大樓                  | 10 / 20       |
| 綜科                      | **3 / 28**    |
| 設計                      | **3 / 24**    |

一般教學大樓幾乎全有；綜科、設計那類多半是實驗室與工作室，學校沒填座位數。

**所以按座位數篩選時不可以把 `null` 悄悄濾掉** —— 濾掉的話使用者會以為綜科整棟
沒有空教室，而事實是我們不知道它多大。`lib/rooms.ts` 的 `filterBySeats()` 把它們
另外回傳，畫面上標成「未提供容量」。

`{semester}/classrooms.json` 裡也有一個 `capacity` 欄位，但它是逐間檢查跑完前的
快照，值會落後 —— **以 `capacity.json` 為準**。

### 1.12 學程與教室只有課號

`programs.json` / `classrooms.json` **只給課號，沒有課程內容**，要顯示得拿課號回
索引查（`lib/crossref.ts`）。

**學程沒有代碼**，識別只能用中文名——這是唯一不能用代碼做交叉連結的地方，
名稱改過舊連結就會失效。

### 1.13 異動事件的型別是**開放**的

`changes.json` 目前有 7 種 `type`：`course_added` / `course_removed` /
`course_changed` / `teacher_added` / `teacher_removed` / `baseline` / `bulk_change`。

**但這個清單會長。** 新增一種 `type` 屬於「新增」，不會升 `schema_version` ——
`teacher_added` 就是這樣冒出來的，而當時的程式碼假設聯集封閉：`KINDS[type]` 回
`undefined`、教師事件又沒有 `teachers` 欄位，`event.teachers.length` 直接讓整個
異動頁掛掉（錯誤頁還顯示成「連不上資料來源」，更難查）。

crawler README 明講：**「`type` 以外的欄位一律當成選填，缺了就降級顯示，不要假設
一定存在。」** 事件是 append-only 的，舊事件確實缺少後來才加的欄位（2026-09-04
之前的 `bulk_change` 沒有 `by_department` / `by_class` / `samples`）。

所以型別把 `type` 以外全部宣告成選填，UI 用**型別守衛**（`isBaseline` /
`isBulk` / `isTeacher`）而不是直接比對 `type` —— 守衛會順便確認該有的欄位真的在。
認不得的型別原樣顯示代碼，不要整頁掛掉。

`baseline` 事件代表「這個學期首次被收錄」，不是學校改了東西。統計「有幾筆異動」
時要排除，否則每加一個歷史學期都會冒出一筆假異動。

### 1.14 空值與已棄用欄位

- 原始頁面用全形空白 `　`(U+3000) 表示「沒有這欄」，crawler 已一律正規化成
  `null`（陣列為 `[]`）。前端不必處理全形空白，但**必須處理 `null`**。
- ⚠️ **`enrolled` / `withdrawn` 在 96-1 以前是整個鍵不存在，不是 `null`。**
  原始課表那時候根本沒有「人」「撤」兩欄。所以型別是 `enrolled?: number | null`，
  判斷有沒有值一律用 `hasEnrolment()`——寫 `=== null` 的話 `undefined` 會溜過去，
  畫面上「修課 N 人」的 N 就整個不見。實測 90-1 ~ 95-2 共 12 個學期沒有這兩欄，
  96-1 起才有。
- `name_en` **永遠是 `null`**（無資料來源），不要在 UI 上留英文課名的位置。
- `quota` 是 `enrolled` 的舊名，值完全相同，已標記 `@deprecated`。
  `enrolled` 是**修課人數不是名額上限**，文案不可寫成「名額」。
- **退選率的分母是「人 + 撤」。** 原始頁面的「人」是**目前**選課人數（撤選的已經
  扣掉），「撤」是目前退選人數 —— 原始修課人次要把撤選的加回來。用 `撤 / 人`
  會系統性高估：某門課撤 17、現存 30，正確算法是 36.2%，`撤/人` 卻是 56.7%。
  （crawler README 說學校沒定義分母；實際語意由本站確認。）

### 1.14b 教師姓名有造字（私用區字元）

實測 115-1 有 **14 位教師**的姓名含私用區碼位，例如教師 23533 在學校頁面上是
`林` + U+E1B3（原始位元組 `E6 9E 97 EE 86 B3`，而頁面自己宣告 UTF-8——
所以那不是解碼錯誤，是學校真的把罕用字存成自訂碼位）。

這種字**沒有標準編碼**，字型檔在學校自己的系統裡，任何瀏覽器都畫不出來，
只會顯示一個空方塊，看起來就像網站壞了。

- 範圍：實測只有教師姓名有（索引 `courses[].teachers[]` 38 個、
  `teachers[].name` 14 個；系所 / 班級 / 教室 / 學程 / 異動全部是 0）。
- 處理：在 `lib/api.ts` 的 `parse()`——也就是**資料進來的那一層**——換成 `〇`
  （`lib/pua.ts`）。不在各元件各做一次：全站十幾個地方會印教師姓名，
  漏一個就是一個空方塊，以後新增的地方也會漏。
- 存進 Cache Storage 的仍是**原始回應**，換字只發生在解析出來的物件上。
- crawler 的 JSON 把這些字**原樣輸出**（不是逸出序列），所以對原始文字
  做取代是有效的。
- 私用區的正規表示式要帶 `u` 旗標並涵蓋三段（基本平面 + 兩個補充平面）：
  少了 `u` 旗標，補充平面的字會被當成兩個代理字元，一個字換出兩個 `〇`。
- 明細頁另外加一句說明那個 `〇` 是什麼——實測 805 位教師沒有人姓名裡本來就有
  `〇`，所以直接比對字元就夠。
- ⚠️ **不要猜那是什麼字。** 教學大綱裡有這些老師的 email，看起來像線索，
  但從 email 反推真人姓名就是編造資料。

### 1.15 相容性策略

crawler 的承諾：**新增欄位不升 `schema_version`；移除欄位 / 改型別 / 改語意才升版。**

所以型別**只宣告用得到的欄位**，也不做 runtime schema 驗證。版本不符時顯示警告
橫幅但**仍照常渲染**——白畫面比欄位顯示怪更糟。

這條在 2026-09-05 派上用場：crawler 升到 v3 改了大綱進度的型別，站上有警告但沒壞。

---

## 2. 架構決策

### 2.1 純靜態 SPA

資料是靜態 JSON，沒有寫入、沒有帳號、沒有伺服器端邏輯。加後端只會多一個會掛的東西。

代價是首屏要等 JS——用路由層級的 code splitting 與 loader 預取壓到可接受。

### 2.2 版本化快取（`lib/api.ts`）

資料放在 GitHub Pages，`Cache-Control: max-age=600` 改不掉——對「舊學期的資料永遠
不會再變」來說太短。做法是把該學期的 `generated_at` 當版本號塞進 query string：

```
GET {BASE}/115-1/index.json?v=2026-09-05T03:34:59Z
```

GitHub Pages 忽略認不得的 query string 照樣回檔案，但瀏覽器與 Cache Storage 會把它
當成不同的 URL。版本沒變就命中快取（零網路請求），版本一變自動抓新的。
同路徑的舊版本會被清掉，快取不會無限成長。

`meta.json` 是唯一例外——它是其他所有端點的版本來源，永遠網路優先，抓不到時退回
快取並在畫面上標示成離線資料。

Cache Storage 不可用（無痕視窗、企業政策）時自動降級成純網路，功能不受影響。

### 2.3 兩層快取互不干涉

Service worker（Workbox）**只管 app shell**，完全不碰 API 資料。兩套快取管同一份
資料只會互相打架，而且只有版本化那層知道 `generated_at`，Workbox 不知道。

`registerType: 'prompt'` 而不是 `autoUpdate`：自動更新會在背景換掉程式碼並重新載入
頁面，而使用者可能正排課排到一半。

### 2.4 搜尋不用索引庫

2,717 筆 × 幾個查詢詞的 `indexOf` 在手機上是毫秒等級；索引庫解決的是十萬筆以上的
問題，差兩個數量級。

而且中文沒有空白分詞，模糊比對按字元算編輯距離會給出大量無關結果——打「白敦文」
就該是找含這三個字的，不要有驚喜。

搜尋跑在 Web Worker 裡，最差情況（空查詢、全部課程）實測 4.7ms。

### 2.5 個人資料寧可備份也不丟掉（`lib/storage.ts`）

課表存的是**課號加一份快照**，不只是課號。這讓兩件事成為可能：離線時直接用快照
畫課表；連上線後把快照跟最新資料比對，停開、調課、換老師、改學分都能標出來。

兩條硬規則：

1. `loadStore()` **永遠不丟例外、永遠回傳可用的結構。** 個人資料出問題不該讓整個
   網站變成白畫面。
2. **絕不靜默丟掉資料。** 整包救不回來時先原樣備份到另一個 key、**再重置主 key**，
   並且在畫面上告訴使用者、提供下載——課表默默變空而沒有任何說明，使用者只會覺得
   網站弄丟了他的東西。

停開的課**留在課表裡但標紅**，不自動移除。替使用者做決定會讓他不知道自己原本選了
什麼。衝堂同理，只警告不阻擋——他可能正在比較兩個方案。

### 2.6 文案只有一份來源

- 免責聲明讀 `meta.disclaimer`，不自己寫第二份。兩份文案遲早會不一致，
  而那一份是資料提供者的正式說法。
- 關於頁的 API 網址從 `lib/api.ts` 匯出，端點清單直接渲染 `meta.endpoints`。
  環境變數改了卻忘了改文案的話，使用者照著頁面上的網址是打不到東西的。

### 2.7 空教室用兩個端點交叉算出來

「某個時段哪些教室沒課」看起來需要每一門課的教室與時段，但那要下載 60 個系所檔
（約 216 KB）。其實兩個現成端點交叉就夠：

| 端點              | 內容               | gzip  |
| ----------------- | ------------------ | ----- |
| `schedule.json`   | 星期 × 節次 → 課號 | 10 KB |
| `classrooms.json` | 教室 → 課號        | 8 KB  |

**某教室在某節有課 ⟺ 它的課號清單與該節次的課號清單有交集。**

框選多格時採「**每一格都要空**」——使用者框連續幾節是想借那一段時間，中間卡一堂
課就不能用了。

⚠️ **「空」只代表課表上沒排課。** 115-1 實測 27.6%（726 門）的課有排時段卻沒登記
教室（體育 185 門、各種「向度」與英文課約 200 門是選課後才分班），那些課實際上還是
佔著教室；教室也可能被借用或鎖著，那些資料學校沒有開放。**這個限制必須顯示在畫面
上**，不能讓人拿著結果去撞門。

### 2.8 退選率：跨學期彙總，並且一定要給基準線

`{semester}/index.json` 就有 `enrolled` / `withdrawn` / `teacher_codes` /
`teachers`，一支檔就夠——**不必再抓 `teachers.json`**：實測 3 個學期 7,956 組
`teacher_codes[i]` ↔ `teachers[i]` 全部對得上，姓名直接從索引取，
跨 39 個學期省下約 3.5 MB。

**期間可選單一學期 / 過去三年 / 過去五年 / 所有期間**，每個學期 83 KB（gzip），
所以「所有期間」約 3.2 MB。三件事讓它撐得住：

- **每個學期取回後立刻縮成摘要**（`summarizeSemester`），原始 JSON 交給 GC。
  51 份索引全展開約 140 MB，縮完之後實測常駐 84 MB。留在 query cache 裡的是
  摘要，不是索引；磁碟上的 Cache Storage 仍存著原始檔，重載不必再下載。
- **只留有人撤選的課**（每學期約 700 門，而不是 3,000 門）。教師則要全部留著，
  因為沒撤選的課仍然算進分母。
- **逐學期漸進顯示**，載入中就先算已到的，並在畫面上寫「已完成 12 / 51 個學期」。

三個非做不可的防呆：

- **小樣本要擋掉。** 不設門檻的話榜首永遠是「8 人修、4 人撤 = 50%」這種課。
  預設門檻是原始修課人次 ≥ 50，可調。
- **本學期通常沒有資料。** 撤選期在學期開始幾週後，`withdrawn` 在那之前全是 0，
  所以預設期間是「過去三年」而不是單一學期。
- **沒有人數欄位的學期要講出來。** 96-1 以前那 12 個學期會被排除在統計外，
  畫面上要寫明是哪幾個，不然使用者只會覺得數字怪。

⚠️ **退選率高不等於老師教得差** —— 可能是擋修的必修、時段不好、或課本身就難。
兩層防誤讀：

- 比率旁邊一律附上「撤 N / M 人」，只給比率就是在鼓勵誤讀。
- **給統計基準線**：平均、中位數、四分位、標準差，並依「平均 + 1 個標準差」
  分成明顯偏高 / 一般 / 偏低。門檻用統計量而不是寫死的百分比——全校平均會隨
  學期與所選期間變動，寫死的數字隔一年就沒有意義。

課程排行**不跨學期合併**：課號每學期都會換（見 §1.6），把不同學期的「工程力學」
併成一列等於自己造一個資料裡沒有的東西。所以課程列各自保留學期標籤。

> 另一個同類網站（`ntut-course.gnehs.net/withdrawal`）用的分母是 `撤 / 人`，
> 所以同一位老師在那邊的數字會比這裡高（侯政伯 47.06% vs 32.0%，
> 分子同樣是 16）。原始人次相同，差別只在分母，見 §1.14。

### 2.9 網址即狀態

搜尋條件（關鍵字、系所、必選修、語言、時段、學分、排序）**全部放在 query string**，
任何查詢結果都能直接複製給別人。輸入框自己維持狀態，debounce 之後才寫回網址——
每個按鍵都推一次歷史紀錄的話，上一頁會變成一個字一個字倒退。

---

### 2.10 字型:Inter 排拉丁、Noto Sans TC 排中文

不指定網頁字型的話,Windows 會拿 Segoe UI 排拉丁、微軟正黑體排中文,
於是「3D列印理論與實務」同一行裡是兩套字體設計,粗細與字面大小都對不起來。

`--font-sans` 的順序就是分工:`Inter` 沒有中文字符,瀏覽器逐字往下找,
中文自然落到 `Noto Sans TC`。後面那串系統字型是網路字型還沒到(或載不到)時的
退路,順序仍然重要——**不能讓它落到明體**。

**用可變字重,不要列舉 400/500/600。** 實測同一個 CJK 分片:

|                      | 每片  | 一頁約 26 片                     |
| -------------------- | ----- | -------------------------------- |
| 可變 `wght@100..900` | 61 KB | 約 1.6 MB                        |
| 靜態單一字重         | 32 KB | 約 0.8 MB(粗體變合成的,中文會糊) |
| 靜態 400 + 500       | 61 KB | 約 1.6 MB(只有兩種字重)          |

可變版跟「兩種靜態字重」同價,但拿到全部字重。Google Fonts 依 `unicode-range`
切片,只會下載頁面真的用到的那幾片。

- `display=swap`:字型還沒到之前先用系統字型顯示,不留白。
- Service worker **不能預先快取**這些檔——build 時不知道會用到哪幾片,
  所以走執行期快取(`runtimeCaching`),字型檔本身永不改版,`CacheFirst` 存一年。
- 匯出課表圖片(`html-to-image`)會自己把字型內嵌進 PNG,實測 302ms 產出
  2400×1756 的圖,字型正確,沒有退回系統字型。

### 2.11 視覺層次靠表面亮度,不靠框線

三條規則,改任何顏色之前先讀:

1. **卡片浮在背景上。** 淺色模式背景 `0.965`、卡片 `1.0`——原本是 `0.978`,
   只差 2.2%,加上很淡的陰影,滿版卡片幾乎貼在背景上分不出邊界。
   **每一個區塊都要在卡片面上**,包括篩選欄;裸的細框線區塊看起來像還沒套樣式。
2. **只有一個強調色**,保留給「可以按」與「目前選中」,不當裝飾。
3. **框線分兩種,因為要求不一樣。** `--border` 是分隔線(`border-b`/`border-t`),
   只要看得出分段;`--input` 是**控制項的外框**(輸入框、下拉、晶片、按鈕),
   那是可操作元件的邊界,WCAG 1.4.11 要求 3:1。硬用同一個值一定顧此失彼——
   舊版兩者都是 0.88~0.9,對白卡只有 1.35:1。注意 Tailwind 的 `border` 是
   **寬度**、`border-input` 是**顏色**,兩個都要寫。
4. **語意色要能當文字用。** 異動頁的徽章是「主色文字 + 同色 15% 淡底」,
   算對比要拿文字色跟**合成後**的淡底比,不是跟卡片比——用卡片算會高估將近
   一個級距(success 實測 4.99 vs 4.07)。
5. **圖示用 `currentColor` 的 SVG,不用 emoji。** emoji 是彩色點陣圖,
   跟一排單色文字擺在一起會像沒做完,而且每個平台長得都不一樣,顏色也控不了。
   `<details>` 的展開箭頭同理,全站共用 `ui/Disclosure.tsx` 的 `Chevron` 與
   `SUMMARY_CLASS`——原生的 ▶ 每家瀏覽器長得不一樣,而只換掉其中一處會更亂。
6. **連結不要永遠加底線。** 課程資訊卡幾乎每個欄位的值都是連結,全部畫底線
   之後整張卡像一頁維基。這些值是定義列表裡的獨立項目、不在句子中間,用主色
   區分就夠,hover 與鍵盤聚焦時再補底線。(句子**中間**的連結仍然要底線。)
7. **主要動作排在同一列的最前面。** 課程頁原本把「加入課表」擠到第二列靠右,
   次要的收藏與複製連結反而跟標題同一行——主次顛倒,看起來也像沒排好。

網格還有一個容易忽略的預設:`items-stretch`。瀏覽頁展開某個系所的班級之後,
同一列旁邊那張卡會被拉成一大塊空白,要 `items-start` 才會各自維持原本的高度。

flex 也有一個:**預設會壓縮子項目**。頁首的導覽原本跟站徽、搜尋框擠在同一個
`flex-wrap` 裡,390px 下每個項目被壓縮 3~4px,於是「空教室」斷成「空教／室」、
「搜尋」斷成「搜／尋」——六個項目全變兩行高,sticky 頁首吃掉 150px。
修法是 `shrink-0` + `whitespace-nowrap`(少任何一個都還是會折行),
並讓導覽在手機上獨立一列、必要時橫向捲動。實測頁首 150px → 101px,
觸控目標 32px → 36px。

課程卡片的高度規則值得記一筆:**多欄時固定、單欄時不固定**。多欄要固定,
否則徽章一行或兩行會讓同一列的卡片高低錯開;多出來的空間用 `mt-auto` 推到
時段與徽章之間,讀起來像段落間距,全部堆在最底下會像沒做完。單欄時左右沒有
鄰居可以對齊,固定高度只是讓每張卡片白白多佔 40px——實測手機上卡片從 168px
降到 121px,同樣的捲動距離多看三成的課。

## 3. 已知陷阱

| 陷阱                                          | 症狀                                 | 對策                                        |
| --------------------------------------------- | ------------------------------------ | ------------------------------------------- |
| `routeTree.gen.ts` 沒進版控                   | CI 型別檢查失敗                      | 進版控，`build` 先跑 `tsr generate`         |
| Workers Assets 用 `_redirects`                | 部署被拒                             | 用 `wrangler.jsonc` 的 `not_found_handling` |
| `overflow-y` 一設，`overflow-x` 跟著變 `auto` | 側欄冒出橫向捲軸                     | 見下                                        |
| sticky 側欄比視窗高                           | 底部選項永遠點不到                   | `max-h` + 內部 `overflow-y-auto`            |
| JSX 的中文換行                                | 畫面多一個空格                       | 中文行尾接中文行首時不要斷行                |
| PNG 匯出的字型                                | 中文變豆腐                           | 只用系統字型堆疊，不用 webfont              |
| 用 `data:` URL 觸發下載                       | iOS Safari 跳「檢視 / 下載」後沒反應 | 改用 `Blob` + `createObjectURL`             |

**CSS 規範規定 `overflow-y` 一旦不是 `visible`，`overflow-x` 就會跟著算成 `auto`。**
所以側欄只要內容凸出幾個像素就會長出橫向捲軸。實測凶手是 `›` 這個文字字元——
它帶著字型的側邊間距，凸出容器右緣 3.33px。改用 inline SVG（邊界精確）
加 `overflow-x-hidden`。

**PNG 匯出不要直接觸發下載。** 一張 200 KB 的圖轉成 `data:` URL 是 29 萬個字元，
iOS Safari 的下載管理員接不住——它會跳出「檢視 / 下載」然後什麼都不做，使用者只會
覺得按鈕壞了。改成 `toBlob()` + `createObjectURL`，並且**先把圖顯示出來再讓使用者
決定怎麼存**：圖在畫面上就一定能長按儲存，那條路不依賴任何 API；分享面板則是 iOS
存進「照片」的正路。

`canShare({ files })` 在桌機 Chrome 也是 `true`，所以**不要自動選**分享或下載——
兩個按鈕都擺出來讓使用者挑。

匯出版面固定寬度 1200px、寫死十六進位色、不吃深色主題——`html-to-image` 是把
computed style 塞進 SVG，顏色函式與 CSS 變數在那條路徑上是額外的風險，而且分享出去
的課表淺色比較好讀。

**其他要講清楚的地方**（都是「不講的話使用者會以為程式壞了」）：

- 異動頁的時間是**本站偵測到**的時刻，不是學校異動的時刻。
- 「檢查時間是今天但沒有事件」＝ 學校真的沒動；「檢查時間停在幾天前」＝ 爬蟲沒在跑。
  兩者必須分得開。
- 學分統計要標示「有幾門沒有標必選修」「有幾門沒有學分數」，否則總和看起來就是錯的。
- 沒有固定時段的課（體育、班週會）要另外列出來，不然使用者會以為課掉了。

---

## 4. 部署

推到 `main` 自動部署到 Cloudflare Workers（Static Assets）。

深層網址（`/course/115-1/364893`）的 SPA fallback 由 `wrangler.jsonc` 的
`not_found_handling: "single-page-application"` 處理。**不是 `_redirects`**——
那是 Pages 的機制，在 Workers Assets 上不合法。

`public/_headers` 設定快取：`/assets/*` 帶內容雜湊，`immutable` 一年；
HTML 與 `sw.js` 不快取。

環境變數只有 `VITE_API_BASE`。資料來源掛掉時可以換成一層代理，程式不用改。

**分析**分成兩件事，別搞混：

- `wrangler.jsonc` 的 `observability` 是**伺服器端**——請求數、錯誤、CPU 時間、
  Workers Logs。但這個 Worker 沒有 `main`（純 Static Assets），沒有程式在跑，
  所以 Logs 是空的。留著是為了哪天真的加了 Worker 邏輯。
- **訪客端**要 Cloudflare Web Analytics，beacon 手動放在 `index.html`。
  自動注入只對 DNS proxy 的網站與 Cloudflare Pages 生效，Workers 自訂網域不算。
  不用 cookie、不做跨站識別；token 是公開的網站識別碼。

---

## 附錄 — 資料驗證指令

改動前先用這些確認假設仍然成立。用 node 是因為這個專案一定裝得起來，
不必另外裝 `jq`：

```bash
BASE=https://tntrock.github.io/ntut-course-crawler
get() { curl -s "$BASE/$1" | node -e "let d='';process.stdin.on('data',c=>d+=c).on('end',()=>{const j=JSON.parse(d);$2})"; }

# 最新學期與收錄數
get meta.json 'console.log(j.latest, j.semesters.length+" 個學期")'

# 節次順序（確認不是字典序）
get meta.json 'console.log(j.periods.map(p=>p.code).join(" "))'

# 授課語言的 distinct 值（篩選選項是動態產生的，值變了要知道）
get 115-1/index.json 'const m={};for(const c of j.courses)m[c.language??"null"]=(m[c.language??"null"]||0)+1;console.log(m)'

# 必選修的 distinct 值
get 115-1/index.json 'console.log([...new Set(j.courses.map(c=>c.requirement_type))])'

# 大綱涵蓋範圍（會一直變，任何寫死的說法都會過期）
get syllabus.json 'console.log("更新中:",j.semesters.map(s=>s.semester).join(" "));console.log("已凍結:",Object.keys(j.frozen??{}).join(" "))'

# 教師代碼數 vs 姓名數（確認同名老師仍存在）
get 115-1/teachers.json 'console.log(j.teachers.length+" 代碼 / "+new Set(j.teachers.map(t=>t.name)).size+" 姓名")'
```
